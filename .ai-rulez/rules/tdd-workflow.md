---
priority: high
---

Write or update tests when changing behavior, especially for bug fixes and public API changes. Prefer targeted tests that prove the changed behavior. Do not run the full test suite as a mandatory commit step unless the user explicitly asks for it or the release procedure requires it.
