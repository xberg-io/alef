/**
 * Plugin interface for {{ interface_name }}.
 *
 * Implement this interface and register an instance with the corresponding
 * TODO(alef-generic-cleanup): Replace extraction-specific generated docs with neutral trait-bridge language.
 * registration function to provide custom behavior for extraction.
 */
interface {{ interface_name }}
{
