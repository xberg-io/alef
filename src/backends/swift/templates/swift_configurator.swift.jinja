{%- if doc %}
{{ doc }}{%- endif %}
    @discardableResult
    public func {{ config_camel }}() -> Self {
        guard var inner = inner else { return self }
        inner.{{ config_camel }}()
        self.inner = inner
        return self
    }
