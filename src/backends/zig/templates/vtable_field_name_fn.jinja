        .name_fn = struct {
            fn thunk(user_data: ?*anyopaque, out_name: ?*?[*c]u8) callconv(.c) void {
                _ = user_data;
                if (out_name) |slot| slot.* = null;
            }
        }.thunk,
