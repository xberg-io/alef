        .version_fn = struct {
            fn thunk(user_data: ?*anyopaque, out_version: ?*?[*c]u8) callconv(.c) void {
                _ = user_data;
                if (out_version) |slot| slot.* = null;
            }
        }.thunk,
