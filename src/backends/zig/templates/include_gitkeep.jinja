// This directory contains C FFI headers needed at link time.
// Headers are copied from the FFI crate during package generation.
// For published distributions, include/ is bundled via the release tarball.
