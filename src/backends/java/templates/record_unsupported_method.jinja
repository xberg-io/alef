    public {% if is_static %}static {% endif %}{{ type_name }} {{ method_name }}({{ params_str }}) {
        throw new UnsupportedOperationException("{{ method_name }} is not yet bridged via JNI; {{ guidance }}.");
    }
