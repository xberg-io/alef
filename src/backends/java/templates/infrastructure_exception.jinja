{{ header }}package {{ package }};

/** {{ doc }} */
@SuppressWarnings("checkstyle:LineLength")
public class {{ class_name }} extends {{ main_class }}Exception {
    /** Creates a new {{ class_name }}. */
    public {{ class_name }}(final String message) {
        super({{ code }}, message);
    }

    /** Creates a new {{ class_name }} with a cause. */
    public {{ class_name }}(final String message, final Throwable cause) {
        super(message, cause);
    }
}
