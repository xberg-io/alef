{# Mirror conversions must be cfg-gated when they reference feature-gated types. #}
{% if source_cfg %}#[cfg({{ source_cfg }})]
{% endif %}impl From<{{ core_ty }}> for {{ name }} {
    fn from(v: {{ core_ty }}) -> Self {
        {{ name }} {
