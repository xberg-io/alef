impl From<{{ binding_name }}> for {{ core_path }} {
    fn from(val: {{ binding_name }}) -> Self {
        match val {
{%- for arm in arms %}
            {{ arm }}
{%- endfor %}
{%- if has_excluded_variants %}
            _ => Default::default(),
{%- endif %}
        }
    }
}
